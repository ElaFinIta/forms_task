import React from "react";

const Header = () => {
    return (
        <div>
            <header>
                <h1>Controlled form example:</h1>
                </header>
        </div>
    );
};

export default Header;